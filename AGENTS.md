# AGENTS.md

本文件约束本仓库内 Agents 与 Skills 的整理、编写和维护方式。

## 编码前思考

- 执行任务前先明确成功标准和验证方式。
- 明确假设；不确定且会影响结果时，先提出一个集中的澄清问题。
- 存在歧义时，列出可能解释，不默默选定单一方案。
- 发现明显更简单的做法时，直接指出并优先采用。
- 发现代码或文档逻辑矛盾、职责重叠时，先暂停收敛并请求澄清。

## 简洁优先

- 用最少的文件和内容解决当前问题。
- 不为一次性需求创建抽象层、复杂架构或占位目录。
- 不为了“未来可能用到”增加扩展性、配置项或重复能力。
- 若现有内容可以大幅精简，优先整理为更清晰的结构。

## 精准修改

- 只修改与当前任务直接相关的文件。
- 不顺手优化相邻内容、排版或无关文档。
- 严格匹配项目现有中文文档风格和 Markdown 结构。
- 因本次修改产生的无效引用、废弃路径和重复说明可直接删除。
- 发现原有死内容或冗余内容时，除非与任务直接相关，否则只在回复中提醒。

## 目录边界

- `product-thinking/` 是产品思维能力域，内容来自互联网一手来源研究。
- `system-architecture-product-design/` 是系统架构能力域，内容来自《系统架构：复杂系统的产品设计与开发》及其提炼材料。
- `architecture-way/` 是《架构之道：软件构建的设计方法》能力域，内容来自 Juval Löwy 的 *Righting Software* 及其官方/出版社资料。
- 各能力域平级维护，不把一个能力域的文件放入另一个能力域目录。
- `software-engineering-process/` 是项目级流程编排层，只负责按软件生命周期路由现有 Skills/Agents、管理阶段门和交接状态，不承载新的书籍方法论。
- 原始资料和中间研究放入对应能力域的 `research.md` 或 `source-materials/`。
- 每个能力域应有自己的 `README.md`、`capability-catalog.md` 和 `agent-contracts.md`。

## Skills 规范

- 每个 Skill 使用目录承载，必须包含 `SKILL.md`。
- Skill 目录名必须与 `SKILL.md` frontmatter 中的 `name` 一致。
- `name` 使用小写字母、数字和短横线，长度不超过 64 个字符。
- `description` 同时说明能力、适用场景和不适用边界。
- `SKILL.md` 只写会改变执行判断的说明；避免通用空话和重复方法。
- 需要 UI 元数据时放在 `agents/openai.yaml`，并确保 `default_prompt` 显式包含 `$skill-name`。
- 新增 Skill 前先检查是否已有能力可以承载，避免拆出重复微技能。

## Agents 规范

- Agent 使用 Markdown 文件，放在对应能力域的 `agents/` 目录。
- Agent frontmatter 至少包含 `name`、`description`、`model` 和 `skills`。
- Agent 正文应明确角色、输入、执行协议、边界、输出契约和交接条件。
- Agent 负责角色和编排，不重复保存 Skill 中的详细方法。
- 下游 Agent 不得把上游假设升级为事实。

## 目标驱动执行

- “修复问题”要转化为：复现问题、定位原因、修改并验证。
- “新增校验功能”要转化为：补充异常输入或边界用例，并确保通过。
- “整理文档”要转化为：目录清晰、链接有效、路径一致、重复内容减少。
- 多步骤复杂任务先输出简短计划，并标注每一步验证方式。

## 验证要求

- 修改 Markdown 后检查本地相对链接是否有效。
- 修改或新增 Skill 后检查 frontmatter、目录命名、`agents/openai.yaml` 和 TODO 占位。
- 移动资料文件时核对内容一致性，必要时用哈希确认。
- 最终回复说明修改了什么、验证了什么，以及仍未处理的风险。

## 输出要求

- 回复使用中文。
- 结构清晰，优先简洁。
- 说明事实、假设、证据、决策和未决问题，不混在一起。
